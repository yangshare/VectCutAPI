# Copy this file to docker/ssl/.htpasswd and replace it with a real htpasswd file.
# Recommended: htpasswd -c docker/ssl/.htpasswd admin
# Non-interactive example (avoid storing secrets in shell history):
# htpasswd -bc docker/ssl/.htpasswd admin YOUR_PASSWORD
